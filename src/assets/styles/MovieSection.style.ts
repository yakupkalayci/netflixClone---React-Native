import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  titles: {
    color: '#fff',
    fontSize: 18,
    fontWeight: 'bold',
    marginBottom: 15,
    marginTop: 20,
  },
});
